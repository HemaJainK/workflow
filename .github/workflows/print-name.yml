name: print-name
on:
  push:
    branches:
      - main
jobs:
  print_name_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Name
        run: echo "Print Name Workflow Triggered Successfully!"

